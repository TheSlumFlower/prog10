#include <stdio.h>
int main()
{
    int a, b, lcm;

    printf("Enter a number:");
    scanf("%d",&a);

    printf("Enter 2nd number:");
    scanf("%d",&b);

    lcm = (a>b)?a:b;
    
    while(lcm%a!=0 || lcm%b!=0)
    {
        lcm++; 
    }
    printf("lcm is %d", lcm);

    return 0;
}
